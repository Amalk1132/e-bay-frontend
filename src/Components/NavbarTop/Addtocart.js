import React, { useEffect, useState } from 'react'

const Addtocart = () => {
  // const [cart,setCart]=useState([])
  // const [total,setTotal]=useState(0)

  // useEffect(()=>{
  //   setTotal(cart.reduce((acc,item)=>acc+parseInt(item.amt),0))

  // },[cart])
  return (
    
    <div>
      {/* {cart.map((item,index)=>{
        return (
          <div key={index}>
            <h1>{item.Price}</h1>
          </div>
        )
      })}
      
      total price{total} */}
      </div>
  )
}

export default Addtocart